import 'chat_mode.dart';

// ai_service_config.dart - Placeholder
class AIServiceConfig {
  final String? apiKey;
  final ChatMode mode;
  const AIServiceConfig({this.apiKey, this.mode = ChatMode.basic});
}
